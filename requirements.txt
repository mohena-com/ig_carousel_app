# Runtime dependencies — Ollama-based Instagram carousel generator
pydantic>=2.0.0
playwright>=1.40.0
jinja2>=3.0.0
qrcode[pil]>=7.4.2
requests>=2.31.0
Pillow>=10.0.0

# Training/fine-tuning dependencies
# Keep these only if you intend to run train.py.
torch>=2.0.0
datasets>=2.0.0
trl>=0.8.0